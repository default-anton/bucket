version: "3.7"

services:
  jellyfin:
    image: ghcr.io/hotio/jellyfin
    ports:
      - "8096:8096"
    environment:
      - PUID=1000
      - PGID=1000
      - UMASK=002
      - TZ
    volumes:
      - /home/anton/jellyfin/config:/config
      - /home/anton/bucket/media:/data
    restart: unless-stopped
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    environment:
      - PUID=1000
      - PGID=1000
      - TZ
      - WEBUI_PORT=8080
    volumes:
      - /home/anton/qbittorrent/config:/config
      - /home/anton/bucket/torrents:/downloads
    network_mode: "container:gluetun"
    restart: unless-stopped
  gluetun:
    container_name: gluetun
    image: qmcgaw/gluetun
    cap_add:
      - NET_ADMIN
    ports:
      - 8080:8080
      - 6881:6881
      - 6881:6881/udp
    environment:
      - VPN_SERVICE_PROVIDER=mullvad
      - VPN_TYPE=wireguard
      - WIREGUARD_PRIVATE_KEY
      - WIREGUARD_ADDRESSES
      - SERVER_COUNTRIES
      - OWNED_ONLY
    restart: unless-stopped
